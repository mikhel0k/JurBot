# Переменные для docker compose (подстановка в docker-compose.yml).
# Проще всего скопировать из backend:  cp backend/.env .env
# Либо задать здесь минимум:

POSTGRES_DB=jurbot
POSTGRES_USER=jurbot
POSTGRES_PASSWORD=your_password
POSTGRES_PORT=5432

REDIS_PORT=6379
MONGO_PORT=27017
MONGO_DB=jurbot
API_TOKEN=
